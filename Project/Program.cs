
using Project;
//using TrainBookingApp;

namespace Project{
    internal static class Program
    {
        /// <summary>
        ///  The main entry point for the application.
        /// </summary>
        [STAThread]
        static void Main()
        {
            ApplicationConfiguration.Initialize();
            //Application.Run(new Update());
            //Application.Run(new availableSeats());
            //Application.Run(new account());
            //Application.Run(new AddTrain());
            //Application.Run(new AddTrip());
            //Application.Run(new UpdateTrip());
            //Application.Run(new BookandCancel());

        }
    }
}